{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node --require ./backend/crypto-polyfill.cjs backend/dist/index.js",
    "healthcheckPath": "/health"
  }
}
